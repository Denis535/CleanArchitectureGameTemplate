// add-skin
add-skin1(key)
    if key != null and key != none
        objects = get-view-skin(key)
        for object in objects
            {object}
add-skin2(key)
    if key != null and key != none
        objects = get-layout-skin(key)
        for object in objects
            {object}
add-skin3(key)
    if key != null and key != none
        objects = get-flex-skin(key)
        for object in objects
            {object}
