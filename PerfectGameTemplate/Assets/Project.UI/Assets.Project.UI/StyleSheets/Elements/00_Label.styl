@import "../StyleSheet__Theme"

// label
label()
    add-skin(label, text: txt(upper-left))
    add-skin2(label)
    add-flex(0, 1, auto, auto)

// label
.unity-label
    label()
    &:focus
        add-skin(focus-effect)
    &.invalid
        add-skin(invalid-effect)
