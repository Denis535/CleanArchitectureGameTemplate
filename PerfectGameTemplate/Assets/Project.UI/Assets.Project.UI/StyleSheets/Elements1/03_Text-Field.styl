@import "../StyleSheet__Theme2"

// text-field
text-field()
text-field-label()
text-field-input()
// text
text(effect)
    add-skin1      effect, component
    add-skin2      effect, component
    add-skin3-flex effect, 1, 1, auto, auto

// text-field
.unity-base-text-field
    text-field()
    &__label
        text-field-label()
    &__input
        text-field-input()
    &__input .unity-text-element
        text(null)
