@import "../StyleSheet__Theme2"

// label
label(effect)
    add-skin1      effect, label-element, create-text(upper-left)
    add-skin2      effect, label-element
    add-skin3-flex effect, 0, 1, auto, auto

// label
.unity-label
    label(null)
