@import "../StyleSheet__Theme"

// box
box()
    add-element-gradient(box, light-box, dark-box, box)
    &.small
        add-element(null, small-box)
    &.medium
        add-element(null, medium-box)
    &.large
        add-element(null, large-box)

// box
.unity-box
    box()
