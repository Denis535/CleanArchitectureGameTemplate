@import "../StyleSheet__Theme2"

// skin
skin--field       = null
skin--field-label = null
skin--field-input = null
// skin
skin--container   = create-layout(null,     auto, auto, 0px, 0px, 0px), create-flex(1, 1, auto, auto), create-flex2(row, flex-start, center)
skin--tracker     = create-layout(null,     auto, 8px,  0px, 1px, 0px), create-flex(1, 1, auto, auto)
skin--dragger     = create-layout(absolute, 8px, 24px,  0px, 1px, 0px), create-flex(0, 1, auto, auto)

// slider-field
slider-field(effect)
    add-misc effect, skin--field
slider-field-label(effect)
    add-misc effect, skin--field-label
slider-field-input(effect)
    add-misc effect, skin--field-input
// container
container(effect)
    add-skin1 effect, element
    add-misc  effect, skin--container
// tracker
tracker(effect)
    add-skin1 effect, component--light
    add-misc  effect, skin--tracker
// dragger
dragger(effect)
    add-skin1 effect, component--light
    add-misc  effect, skin--dragger

// slider-field
.unity-base-slider
    slider-field(null)
    &__label
        slider-field-label(null)
    &__input
        slider-field-input(null)
    &__input #unity-drag-container
        container(null)
    &__input #unity-tracker
        tracker(null)
    &__input #unity-dragger
        dragger(null)
