@import "../StyleSheet__Theme"

// label
label(effect)
    add-skin1      effect, label, create-text(upper-left)
    add-skin2      effect, label
    add-skin3-flex effect, 0, 1, auto, auto

// label
.unity-label
    label(null)
