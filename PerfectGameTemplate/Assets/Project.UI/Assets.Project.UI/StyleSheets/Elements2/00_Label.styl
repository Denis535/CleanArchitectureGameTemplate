@import "../StyleSheet__Theme"

// label
label(effect)
    add-element(effect, label, label, text: create-text(upper-left))
    add-element-flex(0, 1, auto, auto)

// label
.unity-label
    label(null)
